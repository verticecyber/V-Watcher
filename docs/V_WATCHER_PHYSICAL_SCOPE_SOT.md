# V-WATCHER — Physical-Scope SOT (new authority)

> CURRENT AUTHORITY FOR PHYSICAL-SCOPE IMPLEMENTATION
> Generated: 2026-09-12 (read-only audit; zero product modification)
> Baseline: current repo state (post PH-01..PH-05 + hardening pass)
> Claim ceiling: L2 proven (prior, unexpired) + L3-code analysis (this audit). No device claims.
> Supersedes prior gap/next-step docs for physical-scope planning. The frozen 16:00 unified SOT
> remains the historical baseline record; where they conflict on CURRENT state, THIS file wins.

## CURRENT

- Foreground-only deterministic observer; single decision path; honest fallback router; label +
  deny-only homeostasis; flag-grade actuation (1 real action, thread-safe flags); RAM-only memory;
  UI ~80% backend-bound with disclosed residuals; 63/63 L2 suite; lint clean; release artifact
  validated (test-signed); zero network/persistence/secrets/third-party SDK.

## PROVEN

- L2: telemetry fail-closed, routing + fallback bookkeeping, guardrail deny matrix, merge
  semantics, single deterministic entry, ledger bound, feed mirroring, storm flagging, 12 UI
  bindings, intent launch, isReal split, offline/persistence-free posture.
- L3: exact dead-code inventory (§20 of audit), exact threshold conflicts (§12), resolution
  stall mechanics (§10), memory unboundedness (§11), flag layering with prod-dormant neural leg (§7).

## PARTIAL

- Homeostasis (causal deny-only + labels; no hysteresis; neural-leg dormant in deterministic prod).
- UI truth (12/15 + tripwire; DiagnosticScreen:435 null-fallback + defaults + seed bodies residual).
- Memory gates (shape/rate bounded; size unbounded; no delete).
- Storm (flagged; never contained/recovered).
- Bus (transport + 1 subscriber; 12 dead payloads; 7 EMIT_ONLY flows/rings).

## SKELETON

- Nano/Gemma execution (detection honest; inference canned; no deps/weights/runtime).
- NK integrity (full logic, zero callers, counter pinned 0).
- Continuous observing, warmup/release lifecycle wiring, memory cases read path.

## UNUSED

- Listed dead units in audit §20 (classes, methods, branches, payloads, flows, flags, fields).
- `isRealDeviceDataActive` for visibility; sim legacy name branches; orphan interfaces.

## NOT_PROVEN

- Any neural inference; any on-hardware behavior; any timing/perf/battery claim; oscillation
  behavior; cross-death persistence; production feed denial on real hardware; store/policy outcomes.

## DEVICE_REQUIRED

- Nano/AICore program; Gemma artifact + runtime program; thermal/Doze/battery; permission UX;
  inventory scale; death/restore; memory pressure; latency/quota/startup; pre-launch report.

## EXTERNAL

- App ID decision; privacy URL + legal; Console filings (Data Safety, QUERY_ALL_PACKAGES, rating,
  audience, listing); upload key + App Signing; 12×14 if gated; hardware owner; threshold VALUE
  choices; memory persist-vs-RAM decision; NK verdict; resolution-proof redefinition.

## UNKNOWN

- Shadow-vs-hardware telemetry deltas; real-user battery impact; review outcomes; exact
  user-visible effect of 25-vs-30 and 88-vs-92 bands in the wild.
